<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="~/Content/style.css" />
    <style>
        .crema {
            background: #f7e4be;
            color: black;
            border-color: #5B3924;
            border-width: 3px;
            border-style: solid;
        }
    </style>
</head>
<body onload="Desabilitar();" style="background-color: white">

    <h2 style="text-align:center">Reportes</h2>
    <div class="botones text-center">
        <button onclick="location.href = '@Url.Action("", "Reporte/TiemposRequerimiento")'" class="btn  Desarrollador Cliente crema">Comparación tiempos reales y estimados por Requerimiento</button>
        <button onclick="location.href = '@Url.Action("", "Reporte/HorasProyecto")'" class="btn  Desarrollador Cliente crema">Total de Horas estimadas y reales por proyecto</button>
    </div>

    </br>
    </br>

    <div class="botones text-center">
        <button onclick="location.href = '@Url.Action("", "Reporte/Historial")'" class="btn  Desarrollador Cliente crema"> Historial de Desarrollador por proyecto</button>
    </div>


    <script>
        function Desabilitar() {
            var rol = '@ViewBag.user';
            var lista = document.getElementsByClassName(rol);
            for (var i = 0; i < lista.length; ++i) {
                lista[i].hidden = true;
            }
        }
    </script>
</body>
</html>

